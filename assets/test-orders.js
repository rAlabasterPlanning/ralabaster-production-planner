// rAlabaster testorders / tijdelijke testmodus
(()=>{
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function S(){try{return state}catch(_){return null}}
function persist(){try{save()}catch(e){console.error('Testorder opslaan mislukt',e)}}
function ensure(){const s=S();if(!s)return false;s.orders=s.orders||[];s.tasks=s.tasks||[];s.history=s.history||[];s.orderConfirmations=s.orderConfirmations||[];s.quotes=s.quotes||[];s.testOrders=s.testOrders||{enabled:false,baselineOrderIds:[]};s.testOrders.baselineOrderIds=s.testOrders.baselineOrderIds||[];return true}
function mode(){return !!S()?.testOrders?.enabled}
function setMode(on){if(!ensure())return;const s=S();s.testOrders.enabled=!!on;if(on){s.testOrders.baselineOrderIds=s.orders.map(o=>o.id)}else{s.testOrders.baselineOrderIds=[]}persist();decorateOrders();injectSettings()}
function toggleMode(){setMode(!mode())}
function markNewOrders(){if(!ensure()||!mode())return;const s=S(),base=new Set(s.testOrders.baselineOrderIds||[]);let changed=false;for(const o of s.orders){if(!base.has(o.id)){o.isTestOrder=true;base.add(o.id);changed=true}}if(changed){s.testOrders.baselineOrderIds=[...base];persist();decorateOrders()}}
function toggleOrder(id){if(!ensure())return;const o=S().orders.find(x=>x.id===id);if(!o)return;o.isTestOrder=!o.isTestOrder;persist();decorateOrders();window.RALAB_ERP?.renderOrders?.();setTimeout(decorateOrders,60)}
function decorateOrders(){if(!ensure())return;const root=document.getElementById('view-orders');if(!root||root.classList.contains('hidden'))return;
 const toolbar=root.querySelector('.toolbar');
 if(toolbar&&!toolbar.querySelector('.test-mode-toggle')){const b=document.createElement('button');b.className='btn test-mode-toggle'+(mode()?' primary':'');b.textContent=mode()?'TESTMODUS AAN':'Testmodus';b.title='Nieuwe orders automatisch als testorder markeren';b.onclick=toggleMode;const spacer=toolbar.querySelector('.spacer');if(spacer)spacer.after(b);else toolbar.appendChild(b)}else if(toolbar){const b=toolbar.querySelector('.test-mode-toggle');if(b){b.textContent=mode()?'TESTMODUS AAN':'Testmodus';b.className='btn test-mode-toggle'+(mode()?' primary':'')}}
 for(const o of S().orders){const panels=[...root.querySelectorAll('.panel')].filter(p=>(p.textContent||'').includes(o.orderNo||'__no_order__'));for(const p of panels){if(p.dataset.testDecorated===String(!!o.isTestOrder))continue;p.querySelectorAll('.test-order-control,.test-order-badge').forEach(x=>x.remove());const tb=p.querySelector('.toolbar');if(tb&&o.isTestOrder){const badge=document.createElement('span');badge.className='badge test-order-badge';badge.textContent='TEST';badge.style.fontWeight='900';badge.style.border='2px solid currentColor';tb.appendChild(badge)}const action=[...p.querySelectorAll('button')].find(x=>/Order openen/i.test(x.textContent||''))?.parentElement;if(action){const b=document.createElement('button');b.className='btn small test-order-control';b.textContent=o.isTestOrder?'Maak echte order':'Markeer als test';b.onclick=()=>toggleOrder(o.id);action.append(' ',b)}p.dataset.testDecorated=String(!!o.isTestOrder)}}}
}
function cleanup(){if(!ensure())return;const s=S(),tests=s.orders.filter(o=>o.isTestOrder);if(!tests.length)return alert('Er zijn geen testorders om te verwijderen.');const typed=prompt(`Je gaat ${tests.length} testorder(s) en gekoppelde planningsdata definitief verwijderen.\n\nTyp VERWIJDER om door te gaan.`,'');if(typed!=='VERWIJDER')return;const orderIds=new Set(tests.map(o=>o.id)),taskIds=new Set(s.tasks.filter(t=>orderIds.has(t.orderId)).map(t=>t.id));s.orders=s.orders.filter(o=>!orderIds.has(o.id));s.tasks=s.tasks.filter(t=>!orderIds.has(t.orderId));s.orderConfirmations=s.orderConfirmations.filter(x=>!orderIds.has(x.orderId));s.history=s.history.filter(h=>!orderIds.has(h.orderId)&&!taskIds.has(h.taskId));for(const q of s.quotes){if(orderIds.has(q.orderId)){delete q.orderId;if(String(q.status||'').toLowerCase()==='accepted')q.status='concept'}}s.testOrders.enabled=false;s.testOrders.baselineOrderIds=[];persist();alert(`${tests.length} testorder(s) zijn verwijderd. Klanten, prospects en producttemplates zijn behouden.`);injectSettings();window.RALAB_ERP?.renderOrders?.();setTimeout(decorateOrders,60)}
function injectSettings(){if(!ensure())return;const root=document.getElementById('view-settings');if(!root||root.classList.contains('hidden'))return;let box=root.querySelector('#testOrderSettings');if(!box){box=document.createElement('div');box.id='testOrderSettings';box.className='panel';box.style.cssText='padding:16px;margin-top:12px';root.appendChild(box)}const count=S().orders.filter(o=>o.isTestOrder).length;box.innerHTML=`<h3 style="margin-top:0">Testorders</h3><p class="muted">Testorders werken normaal mee in productie en planning, maar kunnen voor livegang in één keer worden opgeschoond.</p><div style="display:flex;gap:8px;align-items:center;flex-wrap:wrap"><button class="btn ${mode()?'primary':''}" onclick="RALAB_TESTORDERS.toggleMode()">${mode()?'TESTMODUS AAN':'Testmodus inschakelen'}</button><button class="btn" onclick="RALAB_TESTORDERS.cleanup()" ${count?'':'disabled'}>Alle testorders verwijderen (${count})</button></div>`}
const obs=new MutationObserver(()=>{markNewOrders();decorateOrders();injectSettings()});
function start(){if(!ensure())return setTimeout(start,200);obs.observe(document.querySelector('main')||document.body,{subtree:true,childList:true,attributes:true,attributeFilter:['class']});setInterval(markNewOrders,700);setTimeout(()=>{decorateOrders();injectSettings()},300)}
window.RALAB_TESTORDERS={toggleMode,setMode,toggleOrder,cleanup,decorateOrders,injectSettings};start();
})();
