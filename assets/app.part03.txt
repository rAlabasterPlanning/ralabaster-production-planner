column" ondragover="dragOver(event)" ondrop="dropToday(event,'')"><h3>Nog in te plannen <span class="hours">${unplanned.length}</span></h3><div class="colbody" data-backlog-container>${renderBacklogSearch()}${renderBacklogGroup('🔴 Direct plannen',g.urgent,true)}${renderBacklogGroup('🟠 Deze week',g.soon,true)}${renderBacklogGroup('🟢 Later',g.later,false)}</div></div>`;
 EMPLOYEES.forEach(emp=>{const arr=entries.filter(x=>x.seg.employee===emp).sort((a,b)=>(a.seg.start||'99:99').localeCompare(b.seg.start||'99:99')||a.task.seq-b.task.seq);const hrs=arr.reduce((a,x)=>a+(Number(x.seg.minutes)||0),0),cap=employeeCapacity(selectedDate,emp,parseDate(selectedDate).getDay()===6);const avail=workWindow(selectedDate,emp);html+=`<div class="column" data-plan-date="${selectedDate}" data-plan-employee="${emp}" ondragover="dragOver(event)" ondrop="dropToday(event,'${emp}')"><h3>${emp}<span class="hours">${fmtMin(hrs)} / ${fmtMin(cap)}</span></h3><div class="muted" style="padding:0 10px 6px">${avail}</div><div class="colbody">${arr.map(x=>taskCard(x.task,false,x.seg)).join('')||'<div class="muted">Geen taken.</div>'}</div></div>`});html+='</div>';root.innerHTML=html;applyBacklogSearch(root,backlogSearchQuery)}
let backlogFocusOrderId='',backlogSearchQuery='';
function focusBacklogOrder(orderId){if(orderId)backlogFocusOrderId=orderId}
function normalizeBacklogSearch(v){return String(v||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().trim()}
function renderBacklogSearch(){return `<div class="backlog-search-wrap"><input class="input" type="search" data-backlog-search-input aria-label="Zoek in te plannen werk" placeholder="Zoek order, product, taak of klant" value="${esc(backlogSearchQuery)}"></div><div class="muted backlog-no-results" data-backlog-no-results hidden>Geen in te plannen werk gevonden.</div>`}
function applyBacklogSearch(scope,query){backlogSearchQuery=query??'';const terms=normalizeBacklogSearch(backlogSearchQuery).split(/\s+/).filter(Boolean),containers=scope.matches?.('[data-backlog-container]')?[scope]:[...(scope.querySelectorAll?.('[data-backlog-container]')||[])];for(const container of containers){const input=container.querySelector('[data-backlog-search-input]');if(input&&input.value!==backlogSearchQuery)input.value=backlogSearchQuery;let visible=0;for(const card of container.querySelectorAll('[data-backlog-order]')){const match=!terms.length||terms.every(x=>normalizeBacklogSearch(card.dataset.backlogSearch).includes(x));card.hidden=!match;if(match){visible++;if(terms.length)card.open=true}}for(const group of container.querySelectorAll('[data-backlog-group]')){const any=[...group.querySelectorAll('[data-backlog-order]')].some(x=>!x.hidden);group.hidden=terms.length&&!any;if(any&&terms.length)group.open=true}const none=container.querySelector('[data-backlog-no-results]');if(none)none.hidden=!terms.length||visible>0}}
document.addEventListener('input',e=>{if(!e.target.matches?.('[data-backlog-search-input]'))return;applyBacklogSearch(e.target.closest('[data-backlog-container]')||document,e.target.value)},true);
function renderBacklogGroup(title,arr,open){const by={};arr.forEach(t=>(by[t.orderId]??=[]).push(t));const groups=Object.values(by).sort((a,b)=>Number(b[0].orderId===backlogFocusOrderId)-Number(a[0].orderId===backlogFocusOrderId)||priorityScore(a[0])-priorityScore(b[0])).map(ts=>{ts.sort((a,b)=>a.seq-b.seq);const o=order(ts[0].orderId),a=orderAnalysis(o),first=ts[0],focused=o.id===backlogFocusOrderId,search=[o.orderNo,o.product,o.project,o.customerName,o.customer,...ts.flatMap(t=>[t.name,t.machine])].filter(Boolean).join(' ');return `<details class="prioritybox" data-backlog-order="${esc(o.id)}" data-backlog-search="${esc(search)}" ${focused?'open':''}><summary><span>${esc(o.orderNo)} – ${esc(o.product)}<br><span class="muted" style="font-weight:500">Deadline ${o.deadline?fmtShort(o.deadline):'-'} · eerst: ${esc(first.name)}</span></span><span>${ts.length} taken · ${feasibilityBadge(o)}</span></summary><div class="inside"><button class="btn primary proposal-start-btn" type="button" data-week-proposal="${esc(o.id)}">Planning voorstel</button>${ts.map(t=>`<div class="backlog-task-wrap">${taskCard(t)}<div class="backlog-task-tools"><button class="btn small" type="button" data-backlog-edit="${esc(t.id)}">Aanpassen</button><button class="btn small danger" type="button" data-backlog-delete="${esc(t.id)}">Verwijderen</button></div></div>`).join('')}</div></details>`}).join('');return `<details class="prioritybox" data-backlog-group ${open||arr.some(t=>t.orderId===backlogFocusOrderId)?'open':''}><summary><span>${title}</span><span>${Object.keys(by).length} orders / ${arr.length} taken</span></summary><div class="inside">${groups||'<div class="muted">Geen taken.</div>'}</div></details>`}
function sortTask(a,b){return (a.start||'99:99').localeCompare(b.start||'99:99')||a.seq-b.seq}
function dragStart(e,id){const t=state.tasks.find(x=>x.id===id);focusBacklogOrder(t?.orderId);e.dataTransfer.setData('text/plain',id);e.dataTransfer.effectAllowed='move'}
function dragOver(e){e.preventDefault();e.currentTarget.classList.add('dropover')}
document.addEventListener('dragleave',e=>{if(e.target?.classList?.contains('column')||e.target?.classList?.contains('daycell'))e.target.classList.remove('dropover')},true)
function confirmWorkSchedule(emp,date){const dow=parseDate(date).getDay();if(dow===0)return confirm('Zondag is geen werkdag. Toch hier plannen?');if(dow===5&&emp!=='Ralph')return confirm(emp+' werkt volgens de ingestelde werkweek niet op vrijdag. Toch plannen?');if(dow===6)return confirm('Zaterdag 08:00–12:00 is optioneel overwerk. '+emp+' hier inplannen als overwerk?');return true}
function dropToday(e,emp){e.preventDefault();e.currentTarget.classList.remove('dropover');const id=e.dataTransfer.getData('text/plain');const t=state.tasks.find(x=>x.id===id);if(!t)return;if(emp){if(!confirmWorkSchedule(emp,selectedDate))return;scheduleTaskAcrossCapacity(t,emp,selectedDate,false,parseDate(selectedDate).getDay()===6)}else{t.employee=null;clearTaskPlanning(t)}save();renderToday()}

function workdaysFrom(anchor,weeks=visibleWeekCount){let out=[],d=parseDate(anchor);while(out.length<weeks*6){if(d.getDay()!==0)out.push(isoDate(d));d.setDate(d.getDate()+1)}return out}
function resetWeekRange(anchor){weekAnchor=anchor;visibleWeekCount=4;renderWeeks()}
function loadMoreWeeks(){if(weekLoadingMore)return;weekLoadingMore=true;const y=window.scrollY;visibleWeekCount+=4;renderWeeks();requestAnimationFrame(()=>{window.scrollTo(0,y);weekLoadingMore=false})}
function watchWeekEnd(){weekScrollObserver?.disconnect?.();const end=document.getElementById('weeksLoadMore');if(!end||typeof IntersectionObserver==='undefined')return;weekScrollObserver=new IntersectionObserver(es=>{if(es.some(e=>e.isIntersecting))loadMoreWeeks()},{rootMargin:'900px 0px'});weekScrollObserver.observe(end)}
function planningWeekKeyLocal(date){const d=parseDate(date);d.setHours(12,0,0,0);d.setDate(d.getDate()+3-((d.getDay()+6)%7));const y=d.getFullYear(),w1=new Date(y,0,4,12),w=1+Math.round(((d-w1)/86400000-3+((w1.getDay()+6)%7))/7);return y+'-W'+String(w).padStart(2,'0')}
function mondayForPlanningWeek(key){const m=String(key||'').match(/^(\d{4})-W(\d{2})$/);if(!m)return'';const y=Number(m[1]),w=Number(m[2]),jan4=new Date(y,0,4,12),dow=(jan4.getDay()+6)%7;jan4.setDate(jan4.getDate()-dow+(w-1)*7);return isoDate(jan4)}
function taskDaysUntilWeek(t){const d=t?.planningWeek?mondayForPlanningWeek(t.planningWeek):'';if(!d)return null;return Math.ceil((parseDate(d)-parseDate(isoDate(new Date())))/86400000)}
function weekTaskRow(t,key){const o=order(t.orderId),days=taskDaysUntilWeek(t),when=days===null?'Geen week':days<=0?'Nu':days===1?'Nog 1 dag':'Nog '+days+' dagen',machine=planningWorkplace(t),setup=/\binstellen\b/i.test((t.name||'')+' '+(t.machine||''));return `<div class="week-pool-task" draggable="true" data-task="${t.id}" ondragstart="dragStart(event,'${t.id}')" onclick="openTask('${t.id}')" style="display:grid;grid-template-columns:30px minmax(0,1fr);gap:7px;align-items:center;padding:7px;border:1px solid rgba(127,127,127,.18);border-radius:8px;background:var(--card,#fff);cursor:grab"><div class="week-task-icon icon-${workplaceKind(machine)}">${planningMachineIcon(machine,setup)}</div><div style="min-width:0"><b style="display:block;white-space:nowrap;overflow:hidden;text-overflow:ellipsis">${esc(t.name||'Taak')}</b><span class="muted" style="display:block">${fmtMin(t.estimate||0)} · ${esc(when)}</span><button class="btn small" type="button" style="margin-top:5px;width:100%" onclick="event.preventDefault();event.stopPropagation();planWeekTaskEarliest('${esc(t.id)}','${esc(key||t.planningWeek||'')}')">Plan taak</button></div></div>`}

function weekOrderGroup(key,tasks){
 const by=new Map();for(const t of tasks){if(!by.has(t.orderId))by.set(t.orderId,[]);by.get(t.orderId).push(t)}
 return [...by.entries()].sort((a,b)=>(Number(order(a[0])?.productionSequence)||9999)-(Number(order(b[0])?.productionSequence)||9999)).map(([orderId,ts])=>{
   ts.sort((a,b)=>(Number(a.seq)||0)-(Number(b.seq)||0));const o=order(orderId),mins=ts.reduce((n,t)=>n+(Number(t.estimate)||0),0);
   return `<details class="week-order-pool" open style="border:1px solid rgba(0,0,0,.12);border-radius:8px;background:#fff;overflow:hidden"><summary style="padding:8px 9px;cursor:pointer"><b>${esc(o?.orderNo||'')} · ${esc(o?.product||'')}</b><div class="muted" style="font-size:11px">${ts.length} taken · ${fmtMin(mins)}</div></summary><div style="padding:0 7px 7px;display:grid;gap:6px"><button class="btn primary small" type="button" onclick="event.preventDefault();event.stopPropagation();planWeekOrderEarliest('${esc(orderId)}','${esc(key)}')">Plan order op eerste moment</button>${ts.map(t=>weekTaskRow(t,key)).join('')}</div></details>`;
 }).join('')
}
function taskPreviousOpen(t){
 const prev=(state.tasks||[]).filter(x=>x.orderId===t.orderId&&!x.deleted&&Number(x.seq)<Number(t.seq)).sort((a,b)=>Number(b.seq)-Number(a.seq))[0]||null;
 if(!prev||t.dependsPrev===false)return null;
 if(['done','completed'].includes(String(prev.status||'').toLowerCase()))return null;
 try{if(taskFinishAt(prev))return null}catch(_){}
 return prev;
}
function planWeekTaskEarliest(id,key,silent=false){
 const t=state.tasks.find(x=>x.id===id&&!x.deleted);if(!t)return false;
 const planner=window.RALAB_DEADLINE_PLANNER;if(!planner)return silent?false:alert('Planner is nog niet geladen.');
 const week=key||t.planningWeek,start=mondayForPlanningWeek(week);if(!start)return silent?false:alert('Deze taak heeft nog geen geldige week.');
 const blocking=taskPreviousOpen(t);if(blocking){if(!silent)alert('Plan eerst de vorige taak: '+(blocking.name||'vorige processtap')+'.');return false}
 const before=JSON.parse(JSON.stringify(t));let cursor=start+'T08:15';
 const prev=(state.tasks||[]).filter(x=>x.orderId===t.orderId&&!x.deleted&&Number(x.seq)<Number(t.seq)).sort((a,b)=>Number(b.seq)-Number(a.seq))[0]||null;
 if(prev){try{const f=taskFinishAt(prev);if(f&&f>cursor)cursor=f}catch(_){}}
 try{
   if(typeof isDryTask==='function'&&isDryTask(t))planner.scheduleWaitStrict?.(t,cursor);
   else if(typeof isExternalTask==='function'&&isExternalTask(t)){const d=cursor.slice(0,10),lead=Number(t.externalLeadDays)||14;t.employee=null;t.planSegments=[];t.date=d;t.start='';t.expectedReturnDate=addDays(d,lead)}
   else planner.allocateInternal?.(t,cursor,{allowPeter:false,allowSaturday:false,preferredEmployee:t.taskEmployeeOverride||t.preferredEmployee||t.employee||undefined});
   const first=(Array.isArray(t.planSegments)?t.planSegments:[]).find(g=>g?.date)?.date||t.date||'';
   if(first&&planningWeekKeyLocal(first)!==week){Object.assign(t,before);if(!silent)alert('In deze week is geen vrije plek voor deze taak.');return false}
   t.lockedPlanning=true;t.manualPlanning=true;t.planningOrigin='week-quick-plan';t.planningWeek=week;
   save();renderWeeks();return true;
 }catch(e){Object.assign(t,before);console.error(e);if(!silent)alert('Taak kon niet automatisch worden ingepland.');return false}
}
function planWeekOrderEarliest(orderId,key){
 const tasks=(state.tasks||[]).filter(t=>t.orderId===orderId&&!t.deleted&&!hasPlanning(t)&&t.planningWeek===key&&!['done','completed','external','in_progress','partial','partly'].includes(String(t.status||'').toLowerCase())).sort((a,b)=>(Number(a.seq)||0)-(Number(b.seq)||0));
 if(!tasks.length)return alert('Er staan geen open taken van deze order meer in deze week.');
 let done=0;for(const t of tasks){if(planWeekTaskEarliest(t.id,key,true))done++;else break}
 save();renderWeeks();if(!done)alert('De eerste taak kon nog niet worden ingepland. Controleer de voorgaande stap of capaciteit.');else if(done<tasks.length)alert(done+' taak/taken ingepland. De volgende taak kon nog niet in deze week.');return done;
}
let weekOrderSearch='';
function setWeekOrderSearch(v){weekOrderSearch=String(v||'').trim().toLowerCase();renderWeeks()}
function weekOrderSearchMatch(t){
 const o=order(t.orderId),hay=[o?.orderNo,o?.customerName,o?.customer,o?.product,o?.project,t?.name,t?.machine].filter(Boolean).join(' ').toLowerCase();
 return !weekOrderSearch||hay.includes(weekOrderSearch);
}
function renderWeekTaskPools(days){
 const visible=[...new Set(days.map(planningWeekKeyLocal))],open=state.tasks.filter(t=>!t.deleted&&!t.isGeneralWork&&!hasPlanning(t)&&!['done','completed','external','in_progress','partial','partly'].includes(String(t.status||'').toLowerCase()));
 const groups=visible.map(key=>({key,tasks:open.filter(t=>t.planningWeek===key&&weekOrderSearchMatch(t)).sort((a,b)=>(Number(order(a.orderId)?.productionSequence)||9999)-(Number(order(b.orderId)?.productionSequence)||9999)||(Number(a.seq)||0)-(Number(b.seq)||0))}));
 const unassigned=open.filter(t=>!t.planningWeek&&weekOrderSearchMatch(t));
 return groups.map((g,i)=>`<details class="weeks-backlog" ${i===0?'open':''} data-week-pool="${g.key}" ondragover="dragOver(event)" ondrop="dropPlanningWeek(event,'${g.key}')"><summary><b>Week ${g.key.slice(-2)}</b><span>${g.tasks.length} taken</span></summary><div class="weeks-backlog-body" style="display:grid;gap:7px">${weekOrderGroup(g.key,g.tasks)||'<div class="muted">Geen los weekwerk meer.</div>'}</div></details>`).join('')+(unassigned.length?`<details class="weeks-backlog"><summary><b>Nog zonder week</b><span>${unassigned.length}</span></summary><div class="weeks-backlog-body" style="display:grid;gap:6px">${unassigned.map(t=>weekTaskRow(t,'')).join('')}</div></details>`:'');
}
function dropPlanningWeek(e,key){e.preventDefault();e.stopPropagation();const id=e.dataTransfer.getData('text/plain'),t=state.tasks.find(x=>x.id===id);if(!t)return;const old=t.planningWeek;t.planningWeek=key;t.planningWeekManual=true;const mins=Math.max(0,Number(t.estimate)||0),o=order(t.orderId);if(o){o.weekCapacityReservations=Array.isArray(o.weekCapacityReservations)?o.weekCapacityReservations:[];if(old){const r=o.weekCapacityReservations.find(x=>x.week===old);if(r)r.minutes=Math.max(0,(Number(r.minutes)||0)-mins)}let r=o.weekCapacityReservations.find(x=>x.week===key);if(!r){r={week:key,minutes:0};o.weekCapacityReservations.push(r)}r.minutes=(Number(r.minutes)||0)+mins;o.weekCapacityReservations=o.weekCapacityReservations.filter(x=>Number(x.minutes)>0).sort((a,b)=>a.week.localeCompare(b.week))}save();renderWeeks()}
function renderWeeks(){const root=document.getElementById('view-weeks'),days=workdaysFrom(weekAnchor),prevFilter=document.getElementById('machineFilter')?.value||'';let html=`<div class="notice"><b>Overzichtplanner:</b> links staat per week wat uitgevoerd moet worden. Sleep een taak naar medewerker + dag om hem echt in te plannen. Sleep een losse taak tussen de weekbakken om de week handmatig te wijzigen.</div><div class="toolbar"><h2>Overzichtplanner</h2><button class="btn" onclick="resetWeekRange(addDays(weekAnchor,-7))">← week</button><input class="input" type="date" value="${weekAnchor}" onchange="resetWeekRange(isoDate(mondayOf(parseDate(this.value))))"><button class="btn" onclick="resetWeekRange(addDays(weekAnchor,7))">week →</button><div class="spacer"></div><button class="btn" id="weekDensityToggle" onclick="weekCompactView=!weekCompactView;renderWeeks()">${weekCompactView?'Uitgebreid overzicht':'Beknopt overzicht'}</button><select id="machineFilter" onchange="renderWeeks()"><option value="">Alle machines</option>${machineOptions(prevFilter)}</select></div><div class="weeks-planner-layout"><aside class="weeks-sidebar"><div class="panel" style="padding:8px;margin-bottom:8px"><input class="input" type="search" placeholder="Zoek order, klant of product" value="${esc(weekOrderSearch)}" oninput="weekOrderSearch=this.value.toLowerCase();renderWeeks()" style="width:100%;box-sizing:border-box"></div><div style="display:grid;gap:8px">${renderWeekTaskPools(days)}</div></aside><div class="weeks-timeline">`;
 const f=prevFilter;
 for(let w=0;w<visibleWeekCount;w++){const ds=days.slice(w*6,w*6+6);html+=`<div class="week-block"><h3>Week ${getWeek(parseDate(ds[0]))}</h3><div class="week-scroll"><div class="weekgrid"><div class="hdr">Medewerker</div>${ds.map(d=>`<div class="hdr">${fmtShort(d)}</div>`).join('')}`;EMPLOYEES.forEach(emp=>{html+=`<div class="person">${emp}</div>`;ds.forEach(d=>{const all=scheduledEntries(d,emp);let arr=all.filter(x=>!f||x.task.machine===f).sort((a,b)=>(a.seg.start||'99:99').localeCompare(b.seg.start||'99:99')||a.task.seq-b.task.seq);let hrs=all.reduce((a,x)=>a+(Number(x.seg.minutes)||0),0),cap=employeeCapacity(d,emp,parseDate(d).getDay()===6),left=cap-hrs,free=n=>n<60?n+' min':fmtMin(n),capacityText=cap<=0?'Geen capaciteit':left>0?'Nog '+free(left):left===0?'Vol':'Overpland '+free(-left),capacityClass=cap<=0?'capacity-none':left>0?'capacity-open':left===0?'capacity-full':'capacity-over';html+=`<div class="daycell" data-plan-date="${d}" data-plan-employee="${emp}" ondragover="dragOver(event)" ondrop="dropWeek(event,'${emp}','${d}')"><span class="hours ${capacityClass}">${capacityText}</span>${arr.map(x=>taskCard(x.task,weekCompactView,x.seg)).join('')}</div>`});});html+='</div></div></div>'}
 html+='<div id="weeksLoadMore" class="weeks-load-more">Volgende weken worden automatisch geladen…</div></div></div>';const activeSearch=document.activeElement?.matches?.('.weeks-sidebar input[type="search"]');const searchPos=document.activeElement?.selectionStart??weekOrderSearch.length;root.innerHTML=html;const sel=document.getElementById('machineFilter');if(sel)sel.value=f;if(activeSearch){const q=root.querySelector('.weeks-sidebar input[type="search"]');if(q){q.focus();try{q.setSelectionRange(searchPos,searchPos)}catch(_){}}}watchWeekEnd();
}
function getWeek(d){d=new Date(Date.UTC(d.getFullYear(),d.getMonth(),d.getDate()));d.setUTCDate(d.getUTCDate()+4-(d.getUTCDay()||7));const y=new Date(Date.UTC(d.getUTCFullYear(),0,1));return Math.ceil((((d-y)/86400000)+1)/7)}
function machineOptions(sel){const ms=[...new Set(state.tasks.map(t=>t.machine).filter(Boolean))].sort();return ms.map(m=>`<option ${m===sel?'selected':''}>${esc(m)}</option>`).join('')}
function dropWeek(e,emp,date){e.preventDefault();e.currentTarget.classList.remove('dropover');const id=e.dataTransfer.getData('text/plain');const t=state.tasks.find(x=>x.id===id);if(!t)return;if(!confirmWorkSchedule(emp,date))return;scheduleTaskAcrossCapacity(t,emp,date,false,parseDate(date).getDay()===6);save();renderWeeks()}

function renderOrders(){const root=document.getElementById('view-orders');let os=state.orders.filter(o=>o.active).slice().sort((a,b)=>(a.deadline||'9999').localeCompare(b.deadline||'9999'));let cards=os.map(o=>{const a=orderAnalysis(o),ts=orderTasks(o.id),done=ts.filter(t=>t.status==='done').length,pct=ts.length?Math.round(done/ts.length*100):0;return `<div class="ordercard clickrow" onclick="openOrder('${o.id}')"><div style="display:flex;justify-content:space-between;gap:8px"><div><b>${esc(o.orderNo)} – ${esc(o.product)}</b><div class="muted">${o.qty} st. · deadline ${o.deadline?fmtShort(o.deadline):'niet ingesteld'}</div></div>${feasibilityBadge(o)}</div><div style="margin-top:9px">Resterend: <b>${fmtMin(a.remaining)}</b>${a.finish?` · verwacht klaar: <b>${fmtShort(a.finish)}</b>`:''}${a.label==='Haalbaar met zaterdagoverwerk'&&a.finishOT?` · met overwerk: <b>${fmtShort(a.finishOT)}</b>`:''}</div><div class="progress"><span style="width:${pct}%"></span></div><div class="muted" style="margin-top:5px">${done}/${ts.length} stappen gereed · volgende: ${nextTaskText(o.id)}</div></div>`}).join('');let rows=os.map(o=>{const ts=orderTasks(o.id),done=ts.filter(t=>t.status==='done').length,pct=ts.length?Math.round(done/ts.length*100):0,a=orderAnalysis(o);return `<tr class="clickrow" onclick="openOrder('${o.id}')"><td><b>${esc(o.orderNo)}</b></td><td>${esc(o.product)}</td><td>${o.deadline?fmtShort(o.deadline):'-'}</td><td>${done}/${ts.length}<div class="progress"><span style="width:${pct}%"></span></div></td><td>${fmtMin(a.remaining)}</td><td>${a.finish?fmtShort(a.finish):'-'}</td><td>${feasibilityBadge(o)}</td><td>${nextTaskText(o.id)}</td></tr>`}).join('');root.innerHTML=`<div class="toolbar"><h2>Orders & haalbaarheid</h2><div class="spacer"></div>${state.deletedTasks?.length?`<button class="btn" onclick="undoDeleteTask()">↶ Verwijderen ongedaan maken</button>`:""}<button class="btn primary" onclick="switchView('import')">Nieuwe order / import</button></div><div class="ordercards">${cards||'<div class="muted">Geen actieve orders.</div>'}</div><div class="panel"><table><thead><tr><th>Order</th><th>Product</th><th>Deadline</th><th>Voortgang</th><th>Resterend</th><th>Verwacht klaar</th><th>Haalbaarheid</th><th>Volgende stap</th></tr></thead><tbody>${rows||'<tr><td colspan=8>Geen actieve orders.</td></tr>'}</tbody></table></div>`}
function nextTaskText(id){const t=orderTasks(id).find(x=>x.status!=='done');return t?`${esc(t.name)} · ${esc(t.machine)}`:'Gereed'}
function openOrder(id){const o=order(id),ts=orderTasks(id),a=orderAnalysis(o);showModal(`<div class="modalhead"><h3>${esc(o.orderNo)} – ${esc(o.product)} – ${o.qty} st.</h3></div><div class="modalbody"><div class="grid3" style="margin-bottom:12px"><div class="field"><label>Deadline</label><input id="oDeadline" class="input" type="date" value="${o.deadline||''}" onchange="updateOrderDeadline('${o.id}',this.value)"></div><div class="field"><label>Haalbaarheid</label><div style="padding-top:8px">${feasibilityBadge(o)}</div></div><div class="field"><label>Resterend / verwacht klaar</label><div style="padding-top:8px"><b>${fmtMin(a.remaining)}</b>${a.finish?' · '+fmtShort(a.finish):''}${a.label==='Haalbaar met zaterdagoverwerk'&&a.finishOT?' · met overwerk '+fmtShort(a.finishOT):''}</div></div></div><div class="panel"><table><thead><tr><th>#</th><th>Bewerking</th><th>Machine</th><th>Begroot</th><th>Planning</th><th>Status</th></tr></thead><tbody>${ts.map(t=>`<tr class="clickrow" onclick="openTask('${t.id}')"><td>${t.seq}</td><td>${esc(t.name)}</td><td>${esc(t.machine)}</td><td>${fmtMin(t.estimate)}</td><td>${isDryTask(t)?(t.waitStartAt?`24/7 · ${fmtShort(dtDate(t.waitStartAt))} ${dtTime(t.waitStartAt)} → ${fmtShort(dtDate(t.waitEndAt))} ${dtTime(t.waitEndAt)}`:'Niet gepland'):isExternalTask(t)?(t.status==='external'?'Extern · retour '+(expectedExternalReturn(t)?fmtShort(expectedExternalReturn(t)):'-'):(t.date?fmtShort(t.date):'Nog niet verstuurd')):(hasPlanning(t)?fmtShort(firstSegment(t).date)+' · '+esc(firstSegment(t).employee||'-')+(taskSegments(t).length>1?' · '+taskSegments(t).length+' dagen':''):'Niet gepland')}</td><td>${statusLabel(t)}</td></tr>`).join('')}</tbody></table></div></div><div class="modalfoot"><button class="btn primary" onclick="closeModal();openPlanEntireOrder('${id}')">Gehele order inplannen</button><div class="spacer"></div><button class="btn" onclick="closeModal()">Sluiten</button></div>`)}

function updateOrderDeadline(id,v){const o=order(id);if(!o)return;o.deadline=v||'';save();closeModal();renderOrders();openOrder(id)}

function renderExternal(){const root=document.getElementById('view-external');const tasks=state.tasks.filter(isExternalTask).filter(t=>t.status!=='done'||t.completedAt).slice().sort((a,b)=>{const aa=expectedExternalReturn(a)||'9999',bb=expectedExternalReturn(b)||'9999';return aa.localeCompare(bb)||a.seq-b.seq});
 const active=tasks.filter(t=>t.status==='external');const pending=tasks.filter(t=>t.status!=='external'&&t.status!=='done');
 function rows(arr,mode){return arr.map(t=>{const o=order(t.orderId);return `<tr><td><b>${esc(o?.orderNo||'')}</b><br>${esc(o?.product||'')} · ${o?.qty||0} st.</td><td><b>${esc(t.name)}</b><br><span class="muted">${esc(t.machine||'')}</span></td><td>${t.externalSentDate?fmtShort(t.externalSentDate):'-'}</td><td>${expectedExternalReturn(t)?fmtShort(expectedExternalReturn(t)):'-'}</td><td>${t.doneQty||0}/${o?.qty||0}</td><td>${statusLabel(t)}</td><td>${mode==='active'?`<button class="btn small primary" onclick="openExternalReceive('${t.id}')">Binnenmelden</button>`:`<button class="btn small" onclick="markExternalSent('${t.id}')">Extern verstuurd</button>`}</td></tr>`}).join('')||'<tr><td colspan="7">Geen regels.</td></tr>'}
 root.innerHTML=`<div class="toolbar"><h2>Extern / binnenmelden</h2><div class="spacer"></div><span class="pill">Bij externe partij: <strong>${active.length}</strong></span></div><div class="notice">Iedere taak van type <b>Extern</b> of waarin <b>“extern”</b> voorkomt wordt als externe bewerking behandeld. Standaard doorlooptijd: <b>14 kalenderdagen</b>. Order, product en volledige bewerkingsomschrijving blijven hier altijd zichtbaar.</div><h3>Bij externe partij</h3><div class="panel"><table><thead><tr><th>Order / product</th><th>Externe bewerking</th><th>Verzonden</th><th>Verwacht retour</th><th>Ontvangen</th><th>Status</th><th></th></tr></thead><tbody>${rows(active,'active')}</tbody></table></div><h3 style="margin-top:18px">Nog extern te versturen</h3><div class="panel"><table><thead><tr><th>Order / product</th><th>Externe bewerking</th><th>Verzonden</th><th>Verwacht retour</th><th>Ontvangen</th><th>Status</th><th></th></tr></thead><tbody>${rows(pending,'pending')}</tbody></table></div>`}
function markExternalSent(id){const t=state.tasks.find(x=>x.id===id),o=order(t.orderId);if(isBlocked(t)&&!confirm('Vorige taak is nog niet gereed. Toch extern versturen?'))return;const lead=Number(t.externalLeadDays)||(CFG.rules?.externalLeadDays||14);const d=isoDate(new Date());t.status='external';t.externalSentDate=d;t.externalLeadDays=lead;t.expectedReturnDate=addDays(d,lead);t.date=d;t.employee=null;t.planSegments=[];save();render();alert(`${o.orderNo} – ${o.product}
${t.name}
Verwacht retour: ${fmtDate(t.expectedReturnDate)}`)}
function openExternalReceive(id){const t=state.tasks.find(x=>x.id===id),o=order(t.orderId);showModal(`<div class="modalhead"><h3>Binnenmelden – ${esc(o.orderNo)} – ${esc(o.product)}</h3></div><div class="modalbody"><div class="notice"><b>${esc(t.name)}</b><br>${esc(t.machine)}<br>${o.qty} stuks · verwacht retour ${expectedExternalReturn(t)?fmtDate(expectedExternalReturn(t)):'-'}</div><div class="grid2"><div class="field"><label>Aantal ontvangen</label><input id="extQty" class="input" type="number" min="0" max="${o.qty}" value="${Math.max(0,o.qty-(t.doneQty||0))}"></div><div class="field"><label>Datum binnen</label><input id="extDate" class="input" type="date" value="${isoDate(new Date())}"></div></div><div class="field" style="margin-top:12px"><label>Opmerking</label><textarea id="extNote">${esc(t.note||'')}</textarea></div></div><div class="modalfoot"><button class="btn" onclick="closeModal()">Annuleren</button><button class="btn primary" onclick="receiveExternal('${id}')">Binnenmelden</button></div>`)}
function receiveExternal(id){const t=state.tasks.find(x=>x.id===id),o=order(t.orderId),q=+document.getElementById('extQty').value,d=document.getElementById('extDate').value,n=document.getElementById('extNote').value;if(q<=0
